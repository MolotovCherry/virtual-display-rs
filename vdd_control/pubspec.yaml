name: vdd_control
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.4.0-227.0.dev <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  dart_vdd:
    path: ../rust/dart_vdd

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
