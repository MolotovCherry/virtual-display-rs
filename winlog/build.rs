//use winresource::WindowsResource;

fn main() {
    // This is commented out, because we can't add winres to another crate
    // and this one at the same time
    //
    // WindowsResource::new()
    //     .set_resource_file("res/eventmsgs.rc")
    //     .compile()
    //     .unwrap();
}
