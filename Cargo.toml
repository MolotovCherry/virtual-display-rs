[workspace]
resolver = "2"
members = [
    "virtual-display-driver",
    "wdf-umdf-sys",
    "wdf-umdf",
    "windebug-logger",
    "driver-ipc",
    "virtual-display-driver-control",
]

[profile.release]
strip = true
codegen-units = 1
lto = true
